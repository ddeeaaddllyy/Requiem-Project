package com.application.requiemproject.utils

/**
 * A separate object for tags.
 * Used to output logs to logcat with a specific header for a specific file.
 */
object TagSet {
    const val SCREEN_CAPTURE_SERVICE_TAG = "ScreenCaptureService"
    const val TEXT_OVERLAY_VIEW_TAG = "TextOverlayView"
    const val SCREEN_CAPTURE_MANAGER_TAG = "ScreenCaptureManager"
    const val OVERLAY_MANAGER_TAG = "OverlayManager"
    const val OCR_REPOSITORY_TAG = "OCRRepository"
}